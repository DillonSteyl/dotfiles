# Dillon's Dotfiles

Dotfiles managed with [DOTBOT](https://github.com/anishathalye/dotbot).

## Secrets

Put shared secrets in `~/.config/shell/secrets.env` using quoted assignments:

```toml
ASDF="SOMETHING"
```
